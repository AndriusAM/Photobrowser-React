import styled from "styled-components";

export const StyledHeader = styled.header`
  text-align: center;
  font-size: 1.2rem;
  
  h1, h2 {
    width: 75%;
    text-align: center;
    margin: 2rem auto;
    padding: 0;
  }
  p {
    font-size: 1rem;
    color: #505050;
  }
`